[package]
name = "kaspa_address_generator"
version = "0.1.0"
edition = "2021"

[dependencies]
kaspa-addresses = { git = "https://github.com/kaspanet/rusty-kaspa" }
blake2b_simd = "1.0"
hex = "0.4"
clap = { version = "4.4", features = ["derive"] }
